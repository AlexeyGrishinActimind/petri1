package com.actimind.petri.runner;

//todo: rename

/**
 * new Walker(NetworkClass)
 *  .withInitialState(network -> {
 *
 *  })
 *  .bind(bind, network -> {
 *
 *    react(network.transition, () -> )
 *
 *    verify(network.place, () -> )
 *
 *  })
 *
 *
 *  run()
 *
 *
 *  interface Test {
 *
 *      void initialState(network)
 *
 *      void bind
 *
 *      @Bind("create user")
 *      void doCreateUser() {
 *          return bind(network.createUser, () -> {
 *
 *          })
 *      }
 *
 *  }
 *
 */
public class SimpleWalker {





}
